{
  "name": "pl-jobhunter-monorepo",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "spec:check": "node scripts/spec-check.js",
    "build": "pnpm -r build"
  },
  "devDependencies": {
    "specify-cli": "^0.1.0"
  }
}
